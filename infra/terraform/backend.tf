# Backend configuration placeholder
# Use `terraform init -backend=false` locally, or pass backend values via `-backend-config` flags.
# Example:
# terraform init -backend-config="bucket=<bucket>" -backend-config="region=<region>" -backend-config="dynamodb_table=<table>"
